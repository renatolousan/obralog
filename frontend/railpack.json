{
    "$schema": "https://schema.railpack.com/railpack.schema.json",
    "steps": {
        "install": {
            "commands": [
                "npm ci"
            ]
        },
        "build": {
            "commands": [
                "npm run build"
            ]
        }
    },
    "deploy": {
        "startCommand": "npm start"
    }
}